# gnoma

Autonomous discovery of genetic and epigenetic interventions for reverse aging using reinforcement learning and biologically grounded world models.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

## Run tests

```bash
pytest
```
